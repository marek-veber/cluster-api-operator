apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: registry-proxy.engineering.redhat.com/rh-osbs/openshift-ose-cluster-kube-cluster-api-rhel9-operator:v4.18
          name: manager
